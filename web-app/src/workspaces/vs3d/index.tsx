/**
 * Viyar Stone Studio (тех. id простору лишається vs3d) — дочка менеджера: «скільки це коштує».
 * Сьогодні це весь застосунок як є; права — `shell/capabilities.ts` → vs3d.
 */
export { default } from '../../App';
