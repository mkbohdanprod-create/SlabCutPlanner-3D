/**
 * Viyar Stone CAD (тех. id простору лишається constructor) — дочка технолога: «як це зробити».
 *
 * v0 (03.09.2026): той самий застосунок, але з правами технолога без ПІНа
 * (метал, послуги для виробництва, припуски, імпорт DXF/SketchUp, старий
 * редактор деталей, паспорт деталі). Усе нове з обучалочки — ТЗ
 * конструктору, ТЗ монтажнику, бланк заміру, калькулятор підкладу, паспорт
 * виробництва, JSON для MES — лягає в підтеки цієї теки, кожне окремим
 * модулем (інваріант 2.48). Див. README.md поруч.
 */
export { default } from '../../App';
