/**
 * ВИДАЛЕНО 27.08.2026.
 *
 * Тут жив самописний експорт у COLLADA (.dae) для SketchUp. Прожив
 * кілька годин: SketchUp 2025 узяв із нього одну плиту без текстур.
 * З'ясувалось, що SketchUp із версії 2023 імпортує GLB нативно
 * (File → Import → GLTF) — тож кнопка «SketchUp» тепер віддає той самий
 * перевірений GLB, що їде в AR (див. engines/arExport.ts і
 * handleSketchupExport у Viewer3D.tsx).
 *
 * Файл можна видалити руками — код навмисно порожній, щоб зламаний
 * імпорт не валив збірку на машині, де видалення файлів обмежене.
 */
export {};
