# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Junk files
old_*.tsx
*_old.tsx
*_test.png
compile_errors.txt

# Локальний dev-оверрайд (публікує порт api для vite-проксі) — не для коміту
docker-compose.dev-override.yml

# Секрети — НІКОЛИ не комітити.
# 10.08.2026 закомічений .env із живими секретами коштував ротації всіх
# ключів. Копія .env.example → .env лежить рівно тут, у папці репозиторію,
# і без цього рядка git закомітить її мовчки.
.env
.env.*
!.env.example
