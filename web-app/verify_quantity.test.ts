// SC-17: це була чернетка, що імпортувала неіснуючий модуль і валила прогін
// тестів окремим червоним файлом. Вміст видалено 18.08.2026; сам файл можна
// стерти вручну (хмарний місток не вміє видаляти файли).
import { describe, it } from 'vitest';

describe.skip('verify_quantity — чернетка, вміст видалено (SC-17)', () => {
  it('noop', () => {});
});
